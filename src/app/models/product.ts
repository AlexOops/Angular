export interface IProduct {
  id?: number, // как необязательный временно
  title: string,
  price: number,
  description: string,
  category: string,
  image: string,
  rating: {
    rate: number,
    count: number
  }
}
